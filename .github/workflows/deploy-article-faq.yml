name: Deploy article-faq to Listenly.fr
on:
  push:
    branches:
      - main
    paths:
      - 'pages/article-faq/**'
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Deploy article-faq via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./pages/article-faq/
          server-dir: /listenly.fr/article-faq/
          exclude: |
            **/.git*
            **/.git*/**
            **/.gitkeep
