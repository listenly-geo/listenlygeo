name: Podcast BTB — edge-podcast

on:
  workflow_dispatch:
  schedule:
    - cron: '0 14 * * 1'

jobs:
  podcast-btb:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Generation fiche podcast (lancement manuel uniquement)
        if: github.event_name == 'workflow_dispatch'
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          RSS_URL: |-
            https://anchor.fm/s/fe9fedf0/podcast/rss
          PODCAST_URL: |-
            https://open.spotify.com/show/0WTH0GU6y38t0ChPIy3dgz
          CONTACT_URL: |-
            https://www.linkedin.com/company/agencelinkx/
          CONTACT_LABEL: |-
            l'agence Linkx.
          LISTENLY_URL: |-
            https://listenly.fr/podcast/show/edge-podcast
          COVER_IMAGE: |-
            https://i.postimg.cc/NjQK0pDw/users2F5z1v-AQ0Mq-TYw-Bwszxqx-Y8B1KCD022Fpages2Fs2Azg-Wog-AJCzz-Zkv-GGAz2Flogo.webp
          PODCAST_SLUG: |-
            edge-podcast
          ACCENT_COLOR: |-
            #2e8bd6
        run: python automation/scripts/generate_podcast_btb.py

      - name: Generation 1 fiche episode max (cron hebdomadaire uniquement, jamais au lancement manuel)
        if: github.event_name == 'schedule'
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          PODCAST_SLUG: |-
            edge-podcast
          MAX_EPISODES: '1'
        run: python automation/scripts/generate_episode_fiches_btb.py

      - name: Commit fiches
        run: |
          git config user.name  "listenly-bot"
          git config user.email "bot@listenly.fr"
          git pull --rebase --autostash origin main
          git add pages/podcast-btb/
          if git diff --staged --quiet; then
            echo "Rien de nouveau."
          else
            git commit -m "Auto: podcast-btb edge-podcast"
            git push
          fi

      - name: Deploy FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server:    ${{ secrets.FTP_SERVER }}
          username:  ${{ secrets.FTP_USERNAME }}
          password:  ${{ secrets.FTP_PASSWORD }}
          local-dir: ./pages/podcast-btb/
          server-dir: /listenly.fr/podcast-btb/
          exclude: |
            **/.git*
            **/.git*/**
            **/.gitkeep
