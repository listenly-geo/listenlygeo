name: Podcast BTB Trafic — Génération quotidienne (tous podcasts)

on:
  workflow_dispatch: {}
  schedule:
    - cron: '0 */3 * * *'   # toutes les 3h — chaque podcast n'est reellement traite qu'une fois/jour (skip si deja fait), les autres passages sont juste des verifications rapides sans cout

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Installer ffmpeg (necessaire pour compression audio Whisper)
        run: |
          if command -v ffmpeg >/dev/null 2>&1; then
            echo "ffmpeg deja present sur le runner : $(ffmpeg -version | head -1)"
          else
            echo "ffmpeg absent, installation via apt-get..."
            export DEBIAN_FRONTEND=noninteractive
            for i in 1 2 3 4 5; do
              sudo apt-get update -qq && sudo apt-get install -y -qq ffmpeg && break
              echo "Tentative $i echouee (verrou apt probable), nouvel essai dans 10s..."
              sleep 10
            done
          fi

      - name: Generer les fiches question du jour (tous podcasts, 1 par podcast max)
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: python automation/scripts/run_all_qa_podcasts.py

      - name: Commit fiches
        if: always()
        run: |
          git config user.name  "listenly-bot"
          git config user.email "bot@listenly.fr"
          git add pages/podcast-btb/
          if git diff --staged --quiet; then
            echo "Rien de nouveau."
          else
            git commit -m "Auto: podcast-btb-qa (run groupe quotidien)"
            git fetch origin main
            if ! git rebase origin/main; then
              echo "::error::Conflit lors du rebase - abandon pour ne pas corrompre le depot."
              git rebase --abort
              exit 1
            fi
            git push
          fi

      - name: Deploy FTP
        if: always()
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server:    ${{ secrets.FTP_SERVER }}
          username:  ${{ secrets.FTP_USERNAME }}
          password:  ${{ secrets.FTP_PASSWORD }}
          local-dir: ./pages/podcast-btb/
          server-dir: /listenly.fr/podcast-btb/
          exclude: |
            **/.git*
            **/.git*/**
            **/.gitkeep
